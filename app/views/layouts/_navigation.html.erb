<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Alpha Blog", root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Articles", articles_path, class: "color" %></li>
        <li><%= link_to "Users", users_path, class: "color" %></li>
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle color" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Actions <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="<%= new_article_path %>">
                  <i class="fa fa-pencil-square-o fa-fw" aria-hidden="true"></i> New Article</a>
              </li>
              <li><a href="#">Another action</a></li>
              <li><a href="#">Something else here</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#">Separated link</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#">One more separated link</a></li>
            </ul>
          </li>
        <% end %>
      </ul>
      <!-- <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="button btn btn-default">Search</button>
      </form> -->
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li><a class="color" href="<%= logout_path %>" data-method="delete">
            <i class="fa fa-sign-out fa-fw" aria-hidden="true"></i> Log out</a></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              <div class="chip color">
                <%= gravatar_for current_user, size: 30 %>
                  <% if current_user.admin? %>Admin<% else %><%= current_user.username %><% end %> <span class="caret"></span>
              </div>
            </a>
            <ul class="dropdown-menu">
              <li><a href="<%= edit_user_path(current_user) %>">
                <i class="fa fa-pencil fa-fw" aria-hidden="true"></i> Edit your profile</a></li>
              <li><a href="<%= user_path(current_user) %>">
                <i class="fa fa-eye fa-fw" aria-hidden="true"></i> View your profile</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#"><i class="fa fa-cog"></i> Settings</a></li>
            </ul>
          </li>
        <% else %>
          <li><a class="color" href="<%= login_path %>">
            <i class="fa fa-sign-in fa-fw" aria-hidden="true"></i> Log in</a></li>
          <li><a class="color" href="<%= signup_path %>">
            <span class="glyphicon glyphicon-user fa-fw"></span> Sign up</a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
